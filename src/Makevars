PKG_CPPFLAGS=-I.
PKG_LIBS=-L. libfuzzy.a
SSDEEP_DIR=ssdeep-2.14.1

all: config.h

config.h:
	cd lib/${SSDEEP_DIR} && ./configure && make
	cd lib/${SSDEEP_DIR} && cp .libs/libfuzzy.a config.h fuzzy.h ../..
	cd lib/${SSDEEP_DIR} && make clean

clean:
#	rm -Rf config.h $(OBJECTS)
